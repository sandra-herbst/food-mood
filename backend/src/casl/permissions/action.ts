/**
 * Enum for generalized strings of entity actions.
 * Used for permissions.
 */
export enum Action {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
